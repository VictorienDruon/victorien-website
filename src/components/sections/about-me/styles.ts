import { css } from "@styled-system/css";

export const paragraph = css({
	textStyle: ["md", "lg", "xl"],
	textAlign: "center",
});

export const highlight = css({
	color: "accent.10",
	fontSize: "inherit",
	lineHeight: "inherit",
});
